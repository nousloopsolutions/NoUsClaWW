{
  "name": "rabbit-hole",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "wrangler": "^3.80.0",
    "typescript": "^5.5.0"
  }
}
